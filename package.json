{
  "name": "stephane-auer-3dprint-models-site",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "thumbs": "node scripts/generate-thumbs.mjs",
    "thumbs:alien": "node scripts/generate-thumbs.mjs --only=alien1"
  },
  "devDependencies": {
    "playwright": "^1.51.0",
    "vite": "^6.2.0"
  },
  "dependencies": {
    "three": "^0.183.2"
  }
}
